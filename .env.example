# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080/api
NEXT_PUBLIC_APP_NAME=Streamline

# Environment
NODE_ENV=development

# Optional: JWT Secret for client-side validation (if needed)
# NEXT_PUBLIC_JWT_SECRET=your-jwt-secret-here

# Optional: Upload configurations
# NEXT_PUBLIC_MAX_FILE_SIZE=5242880
# NEXT_PUBLIC_ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf